server:
  port: 9091

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taxcalculator
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true


tt:
  taxcodes:
    default: 1257L
